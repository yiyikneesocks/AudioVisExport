# INBOX — 你的输入箱（文档 1：**只有你写**，AI 只读、绝不编辑本文件）

> **规则（AI 必须遵守）**：本文件由用户独占写入。AI **永远不要**用 `write`/`edit` 修改本文件
> （否则会把用户新写的内容冲掉）。AI 的正确流程见 `docs/INBOX_WORKLOG.md` 顶部说明。
>
> **用法（给你）**：把要解决的问题 / 新需求，**直接在下面「📥 新任务」里往下加**，
> 一行一条或一段都行，不用讲格式。想催就 `!!` 开头，拿不准要不要做就 `?` 开头。
>
> AI 处理完会在聊天里告诉你「哪几条可以删了」，你再把对应行删掉即可；
> 忘了删也没关系（AI 靠工作台账去重，不会重做）。

---

## 📥 新任务

（在这里往下写 ↓）
